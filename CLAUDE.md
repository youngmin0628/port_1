# 선착순 쿠폰 발급 시스템

## 프로젝트 목적

이 저장소는 **학습 겸 포트폴리오** 프로젝트다. 목표는 "동작하는 쿠폰 서비스"가 아니라
**"동시성 문제를 재현하고, 단계별로 해결하며, 개선을 수치로 증명하는 과정"** 자체다.

따라서 다음 원칙이 다른 모든 것에 우선한다.

> **측정 없는 개선은 이 프로젝트에서 개선이 아니다.**
> 성능 관련 코드를 바꿨다면 반드시 before/after 수치를 `docs/benchmark.md`에 남긴다.

## 절대 하지 말 것

1. **단계 건너뛰기 금지.** 아래 로드맵의 현재 단계만 작업한다.
   Phase 1을 하는 중에 "어차피 나중에 Redis 쓸 거니까" 하며 Redis 코드를 미리 넣지 않는다.
   **나이브한 구현의 실패를 먼저 재현해야 다음 단계에 의미가 생긴다.**
2. **선제적 추상화 금지.** `CouponIssueStrategy` 인터페이스 같은 걸 구현체 1개일 때 만들지 않는다.
   구현체가 2개 이상 실제로 필요해진 시점에 추출한다.
3. **요청하지 않은 기능 추가 금지.** 관리자 API, Swagger, 로그인, 소프트 딜리트 등
   지시에 없으면 만들지 않는다. 필요해 보이면 코드로 쓰지 말고 말로 제안한다.
4. **불필요한 방어 코드 금지.** 발생 불가능한 시나리오의 try-catch, null 체크를 넣지 않는다.
5. **인접 코드 개선 금지.** 요청과 무관한 포맷팅/리팩터링/주석 수정은 하지 않는다.
   죽은 코드를 발견하면 삭제하지 말고 보고한다.

## 작업 방식

### 항상 계획 먼저

구현 전에 다음을 먼저 제시하고 승인을 받는다.

```
1. [작업] → 검증: [어떻게 확인할 것인가]
2. [작업] → 검증: [어떻게 확인할 것인가]
```

가정이 필요하면 명시한다. 해석이 여러 개면 고르지 말고 나열한다. 막히면 멈추고 무엇이 불명확한지 말한다.

### 테스트 우선 (이 프로젝트에서는 필수)

동시성/성능 작업은 **실패하는 테스트를 먼저 작성**한다.

- "재고 차감 구현" → ❌
- "1000 스레드가 동시 요청할 때 100개 한정 쿠폰이 100개만 발급되는지 검증하는 테스트 작성 →
  실패(오버셀) 확인 → 통과시키기" → ⭕

동시성 테스트는 `ExecutorService` + `CountDownLatch` 조합을 쓴다.
단일 스레드 테스트로 동시성 문제를 검증했다고 주장하지 않는다.

### 보고는 짧게

결과부터 말한다. 무엇을 할 예정인지 서술하지 않는다. 완료된 단계는 한 줄씩.
이미 본 파일 전체를 다시 출력하지 않고 변경된 부분만 보여준다.
이유 설명은 트레이드오프가 있었거나 예상과 다르게 동작했을 때만 한다.

---

## 로드맵

현재 단계는 `docs/progress.md` 상단에 기록한다. 단계 완료 조건을 만족하지 못하면 다음으로 넘어가지 않는다.

### Phase 1 — 나이브 구현 + 실패 재현

가장 단순한 JPA 구현: `count()`로 발급 수 조회 → 재고 확인 → `save()`.
락 없음, 캐시 없음, 큐 없음.

- 완료 조건: 동시성 테스트에서 **오버셀이 실제로 발생**하고, 그 로그/수치가 문서에 남아있을 것
- k6로 부하 테스트 후 TPS, p95/p99 latency 기록

### Phase 2 — DB 락으로 해결 (비교군)

비관적 락(`SELECT ... FOR UPDATE`), 낙관적 락, unique 제약을 각각 적용해보고 비교한다.

- 완료 조건: 정합성은 확보되지만 **TPS가 Phase 1보다 떨어지는 것**을 수치로 확인
- 이 비교군이 "왜 Redis를 쓰는가"에 대한 유일한 근거다. 생략하면 프로젝트의 절반이 사라진다.

### Phase 3 — Redis 원자 연산

Lua 스크립트로 **재고 차감 + 중복 발급 방지를 단일 원자 연산**으로 처리.

- `INCR` 후 한도 초과분 롤백 방식과 Lua 방식의 차이를 이해하고 선택 이유를 문서화
- 중복 발급 방지는 Set 또는 `SET NX` 사용
- Redisson 분산락과 Lua 스크립트의 성능/특성 비교
- 주의: Redis와 DB 사이 정합성이 깨지는 지점(Redis 성공 후 앱 다운)을 인지하고 문서화
- 완료 조건: 오버셀 0건 + Phase 2 대비 TPS 개선 수치

### Phase 4 — Kafka 비동기화

Redis 검증 통과 → Kafka produce → 컨슈머가 DB insert. API 응답은 "발급 접수" 시점에 반환.

반드시 다뤄야 할 문제들:
- **멱등성**: at-least-once 전달로 인한 중복 컨슈밍 → 멱등키 설계 (DB unique 제약 병행)
- **파티션 키**: 쿠폰 ID 기준 vs 유저 ID 기준, 순서 보장이 필요한지 판단
- **실패 처리**: 재시도, DLQ, 그리고 Redis 재고는 깎였는데 DB 저장에 실패한 경우의 보상
- **컨슈머 lag** 모니터링

- 완료 조건: p99 응답시간 개선 수치 + 컨슈머 lag이 유입량을 따라잡는지 확인 + DLQ 동작 검증

### Phase 5 — MSA 분리

Phase 4까지의 병목을 근거로 서비스를 나눈다. **"쪼개고 싶어서"가 아니라 "이 경계에서 이런 이유로"** 나눈다.

- 최소 구성: `coupon-service`, `notification-service` (각자 DB 소유)
- 통신은 Kafka 이벤트 기반. 동기 호출이 꼭 필요한 곳만 REST.
- 이벤트 스키마 계약 정의 (Avro/JSON Schema 중 택1, 선택 이유 문서화)
- 분산 추적(Micrometer Tracing + Zipkin)으로 요청 흐름 추적
- 서비스 하나가 죽었을 때 나머지가 어떻게 동작하는지 실험하고 기록
- API Gateway, 서비스 디스커버리는 **필요성이 증명된 후에만** 추가

---

## 기술 스택

Java 21 / Spring Boot 3.x / Gradle / JPA / MySQL / Redis / Kafka
인프라는 Docker Compose로 로컬 구성. 부하 테스트는 k6.

## 코드 컨벤션

- 도메인 로직은 엔티티/도메인 서비스에, 컨트롤러는 얇게
- 트랜잭션 경계를 명시적으로 인지: `@Transactional` 안에서 외부 호출(Redis/Kafka) 하지 않기
- 테스트는 Testcontainers 사용 (임베디드 Redis/Kafka는 실제 동작과 차이가 있음)
- 기존 코드 스타일을 따른다. 본인 취향으로 바꾸지 않는다.

## 문서화 규칙

`docs/benchmark.md`에 각 Phase마다 아래 형식으로 누적 기록한다.

| Phase | 방식 | TPS | p95 | p99 | 오버셀 | 비고 |
|-------|------|-----|-----|-----|--------|------|

테스트 조건(스레드 수, duration, 쿠폰 수량, 하드웨어)을 같이 적는다.
조건이 다른 수치를 비교하지 않는다.
