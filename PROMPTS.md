# Claude Code 단계별 프롬프트

`CLAUDE.md`를 프로젝트 루트에 두고, 아래 프롬프트를 순서대로 사용한다.
**한 프롬프트가 끝나고 검증이 통과한 뒤에만** 다음으로 넘어간다.

---

## 프롬프트 작성 3원칙

1. **결과가 아니라 검증 방법을 준다.** "재고 차감 구현해줘"가 아니라 "이 테스트를 통과시켜줘".
2. **한 번에 하나.** 여러 Phase를 한 프롬프트에 담으면 전부 얕게 나온다.
3. **계획을 먼저 받는다.** 큰 작업은 Shift+Tab으로 plan mode에 들어가서 계획부터 검토한다.

---

## Phase 0 — 프로젝트 셋업

```
선착순 쿠폰 발급 시스템의 뼈대를 잡을 거야. CLAUDE.md를 먼저 읽어줘.

이번 작업 범위는 Phase 0 셋업까지만이야. Redis나 Kafka 코드는 아직 넣지 마.

- Spring Boot 3.x, Java 21, Gradle 멀티모듈 아님(단일 모듈로 시작)
- 도메인: Coupon(id, name, totalQuantity), CouponIssue(id, couponId, userId, issuedAt)
- MySQL, Redis, Kafka를 띄우는 docker-compose.yml (Redis/Kafka는 컨테이너만, 앱 연동은 아직 X)
- 헬스체크 엔드포인트

먼저 계획을 1~3단계로 제시하고 내 승인을 받은 다음 구현해줘.
```

**검증**: `docker compose up -d` 후 앱이 뜨고 헬스체크 200.

---

## Phase 1 — 나이브 구현 + 오버셀 재현 ⭐ 가장 중요

```
Phase 1을 시작할게. 목표는 "잘 동작하는 구현"이 아니라 "동시성 문제를 재현하는 것"이야.

1) 가장 단순한 쿠폰 발급 로직을 구현해줘.
   - couponIssueRepository.countByCouponId()로 발급 수 조회
   - totalQuantity와 비교
   - 통과하면 save()
   - 락, 캐시, 큐 전부 사용 금지. 일부러 취약하게 만드는 거야.

2) 동시성 테스트를 작성해줘.
   - Testcontainers로 MySQL 사용
   - 총 수량 100개인 쿠폰에 1000명이 ExecutorService + CountDownLatch로 동시 요청
   - 최종 발급 건수가 100인지 assert

3) 테스트를 실행해서 실패(오버셀)를 보여주고, 실제 발급된 건수를 알려줘.
   이 테스트는 지금 실패하는 게 정상이야. 통과시키려고 고치지 마.

4) 왜 이 문제가 발생하는지 read-modify-write 관점에서 3줄로 설명해줘.
```

**검증**: 테스트가 실패하고, 발급 건수가 100을 초과한다. (예: 137건)
이 수치를 `docs/benchmark.md`에 기록.

```
k6 부하 테스트 스크립트를 작성하고 Phase 1 성능을 측정해줘.
- 시나리오: 30초간 VU 200으로 발급 요청
- TPS, p95, p99를 docs/benchmark.md 표에 기록
- 테스트 조건(VU 수, duration, 쿠폰 수량)도 같이 적어줘
```

---

## Phase 2 — DB 락 (비교군)

```
Phase 2야. Phase 1의 오버셀을 DB 락으로 해결하고, 그 대가를 측정할 거야.

세 가지를 각각 별도 구현체로 만들고 동일한 동시성 테스트를 통과시켜줘.
a) 비관적 락 (SELECT FOR UPDATE)
b) 낙관적 락 (@Version + 재시도)
c) DB unique 제약 (userId + couponId)

각각에 대해:
- 동시성 테스트 통과 확인
- k6로 Phase 1과 동일 조건으로 측정
- benchmark.md에 세 줄 추가

마지막으로 세 방식의 트레이드오프를 표로 정리해줘. (정합성/성능/구현복잡도/한계)
```

**검증**: 오버셀 0건, TPS는 Phase 1보다 떨어져 있어야 정상. 안 떨어졌으면 부하가 부족한 것.

---

## Phase 3 — Redis 원자 연산

```
Phase 3야. Redis로 재고 관리를 옮길 거야.

먼저 설계 질문에 답해줘 (코드는 아직 쓰지 마):
1. INCR 후 초과분 롤백 방식과 Lua 스크립트 방식의 차이는? 어느 쪽을 왜 선택해야 해?
2. 중복 발급 방지를 Redis Set으로 할 때 메모리 사용량이 어떻게 되지? 대안은?
3. Redis에서 재고 차감 성공 직후 앱이 죽으면 어떤 상태가 되지?

답을 검토한 뒤 구현 지시할게.
```

이어서:

```
논의한 대로 Lua 스크립트로 구현해줘.
- 재고 차감 + 중복 발급 체크를 단일 원자 연산으로
- 기존 동시성 테스트 통과 (Testcontainers Redis 사용)
- k6 동일 조건 측정 후 benchmark.md 기록
- 3번 질문의 정합성 취약점을 docs/known-issues.md에 기록 (지금 해결하진 말고)
```

**선택 과제**: `Redisson 분산락으로도 구현해서 Lua 스크립트와 성능 비교해줘. 언제 분산락이 더 적절한지도 정리해줘.`

---

## Phase 4 — Kafka 비동기화

```
Phase 4야. Redis 검증 통과 후 DB 저장을 Kafka로 비동기화할 거야.

구현 전에 설계부터 잡아줘:
- 토픽/파티션 설계와 파티션 키를 무엇으로 할지, 순서 보장이 필요한지
- 컨슈머가 중복으로 메시지를 받았을 때 (at-least-once) 중복 발급을 어떻게 막을지
- 컨슈머에서 DB 저장이 실패하면 Redis 재고는 어떻게 되는지, 보상 전략

설계 승인 후 구현할게.
```

이어서:

```
구현해줘. 완료 조건은 아래 전부야.
1. 동시성 테스트 통과 (비동기라 컨슈머 처리 완료까지 대기하는 로직 필요 - Awaitility 사용)
2. 컨슈머 중복 수신 시나리오 테스트: 같은 메시지 2번 처리해도 발급 1건인지 검증
3. DLQ 동작 검증: 컨슈머에서 예외 발생시켜 DLQ로 가는지 테스트
4. k6 측정 → p99 응답시간 개선을 benchmark.md에 기록
5. 컨슈머 lag 확인 방법을 README에 적어줘
```

---

## Phase 5 — MSA 분리

```
Phase 5야. 근데 그 전에 물어볼게.

지금까지의 코드에서 서비스를 나눈다면 어디를 경계로 잘라야 할까?
"MSA니까 나눈다"가 아니라 지금 코드의 실제 문제(배포 단위, 부하 특성, 데이터 소유권)를
근거로 2~3개 후보를 제시하고 각각의 장단점을 알려줘. 코드는 아직 쓰지 마.
```

이어서 (합의된 경계로):

```
합의한 대로 coupon-service와 notification-service로 분리해줘.

- Gradle 멀티모듈, 각 서비스가 자기 DB만 소유 (직접 조인 금지)
- 서비스 간 통신은 Kafka 이벤트. 이벤트 스키마 계약을 별도 모듈로 분리
- 발급 완료 이벤트 → notification-service가 구독해서 알림 처리
- Micrometer Tracing + Zipkin으로 서비스 간 요청 추적

완료 후: notification-service를 강제로 내렸을 때 coupon-service가 정상 동작하는지
실험하고 결과를 docs/resilience.md에 기록해줘.
```

---

## 자주 쓸 보조 프롬프트

**막혔을 때**
```
지금 이 문제의 원인을 추측하지 말고, 확인할 수 있는 가설 3개를 세우고
각각을 어떻게 검증할지 알려줘. 검증 전에 코드 수정하지 마.
```

**Claude가 과하게 만들었을 때**
```
이 구현 지금 몇 줄이야? 요구사항을 만족하는 최소 버전으로 다시 써줘.
추상화와 방어 코드 걷어내고, 무엇을 왜 지웠는지 한 줄씩 알려줘.
```

**포트폴리오 정리 단계**
```
benchmark.md의 전체 수치를 바탕으로 README를 작성해줘.
각 Phase에서 "어떤 문제 → 어떤 시도 → 어떤 결과(수치)"가 드러나야 하고,
실패했거나 해결 못 한 것도 known-issues로 솔직하게 남겨줘.
```

---

## Claude Code 사용 팁

- `CLAUDE.md`는 프로젝트 루트에 두면 세션 시작 시 자동으로 읽힌다. `/memory`로 현재 로드된 메모리 파일을 확인할 수 있다.
- 큰 작업은 **plan mode**(Shift+Tab)로 계획을 먼저 검토한 뒤 실행시킨다.
- 세션 중에 알게 된 규칙은 `#`로 시작하는 메시지로 즉석 추가하고, 계속 쓸 규칙이면 CLAUDE.md에 옮긴다.
- Phase가 바뀔 때마다 CLAUDE.md의 "현재 단계"를 갱신한다. 안 하면 Claude가 다음 Phase 코드를 미리 써버린다.
- 공식 문서: https://code.claude.com/docs/en/memory
